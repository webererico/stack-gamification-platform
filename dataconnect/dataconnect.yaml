specVersion: "v1"
serviceId: "stack-up"
location: "us-central1"
schema:
  source: "schema"
  datasource:
    postgresql:
      database: "stackup"
      cloudSql:
        instanceId: "stack-up-fdc"
connectorDirs: ["connector"]
